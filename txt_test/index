code = Column('code', String(20), primary_key=True)  # 股票代码
    date = Column('date', Date, index=True, primary_key=True)  # 最新日期

    maLong = Column('maLong', Integer, default=0)  # 多头排列
    maCrossAbove_5_10 = Column('maCrossAbove_5_10', Integer, default=0)  # 5日均线上穿10日均线
    maCrossAbove_5_20 = Column('maCrossAbove_5_20', Integer, default=0)  # 5日均线上穿20日均线
    priceBreak_5 = Column('priceBreak_5', Integer, default=0)  # 收盘价上探5日均线
    priceBreak_10 = Column('priceBreak_10', Integer, default=0)  # 收盘价上探10日均线
    priceBack_10 = Column('priceBack_10', Integer, default=0)  # 收盘价回踩10日均线
    priceBack_20 = Column('priceBack_20', Integer, default=0)  # 收盘价回踩20日均线
    kdjGoldenCross = Column('kdjGoldenCross', Integer, default=0)  # KDJ金叉
    kdjDeadCross = Column('kdjDeadCross', Integer, default=0)  # KDJ死叉
    kdjHighGoldenCross = Column('kdjHighGoldenCross', Integer, default=0)  # KDJ高位金叉
    kdjLowGoldenCross = Column('kdjLowGoldenCross', Integer, default=0)  # KDJ低位金叉
    kdjHighDeadCross = Column('kdjHighDeadCross', Integer, default=0)  # KDJ高位死叉
    kdjLowDeadCross = Column('kdjLowDeadCross', Integer, default=0)  # KDJ低位死叉
    bollGapIncrease = Column('bollGapIncrease', Integer, default=0)  # 布林通道开口变大
    bollGapDecrease = Column('bollGapDecrease', Integer, default=0)  # 布林通道开口变小
    bollCrossAboveMiddleBand = Column('bollCrossAboveMiddleBand', Integer, default=0)  # 收盘价上穿BOLL中轨
    bollCrossAboveUpperBand = Column('bollCrossAboveUpperBand', Integer, default=0)  # 收盘价下破BOLL中轨
    bollCrossBelowMiddleBand = Column('bollCrossBelowMiddleBand', Integer, default=0)  # 收盘价下破BOLL中轨
    bollCrossBelowLowBand = Column('bollCrossBelowLowBand', Integer, default=0)  # 收盘价下破BOLL下轨
    macdGoldenCross = Column('macdGoldenCross', Integer, default=0)  # MACD金叉
    macdDeadCross = Column('macdDeadCross', Integer, default=0)  # MACD死叉
    macdBullishDivergences = Column('macdBullishDivergences', Integer, default=0)  # MACD顶背离
    macdBearishDivergences = Column('macdBearishDivergences', Integer, default=0)  # MACD底背离
    macdUpZeroGoldenCross = Column('macdUpZeroGoldenCross', Integer, default=0)  # MACD零轴以上金叉
    macdDownZeroGoldenCross = Column('macdDownZeroGoldenCross', Integer, default=0)  # MACD零轴以下金叉
    macdUpZeroDeadCross = Column('macdUpZeroDeadCross', Integer, default=0)  # MACD零轴以上死叉
    macdDownZeroDeadCross = Column('macdDownZeroDeadCross', Integer, default=0)  # MACD零轴以下死叉
    macdEnlarge = Column('macdEnlarge', Integer, default=0)  # MACD逐步放大
    macdDecrease = Column('macdDecrease', Integer, default=0)  # MACD逐步减小
    volumeAtrophy = Column('volumeAtrophy', Integer, default=0)  # 缩量
    volumeModerateIncrease = Column('volumeModerateIncrease', Integer, default=0)  # 温和放量
    volumeObviousIncrease = Column('volumeObviousIncrease', Integer, default=0)  # 明显放量
    volumeHugeIncrease = Column('volumeHugeIncrease', Integer, default=0)  # 巨量放量
    volumeGodIncrease = Column('volumeGodIncrease', Integer, default=0)  # 天量
    ltm = Column('ltm', Integer, default=0)  # 涨跌停标记
    beta = Column('beta', Numeric(20, 4))  # beta值


    maShort = Column('maShort', Integer, default=0)  # 空头排列
    maBond = Column('maBond', Integer, default=0)  # 均线粘合
    ma5IncreaseX = Column('ma5IncreaseX', Integer, default=0)  # 5日均线连续上涨X天
    priceBack_5 = Column('priceBack_5', Integer, default=0)  # 收盘价回踩5日均线
    ma10IncreaseX = Column('ma10IncreaseX', Integer, default=0)  # 10日均线连续上涨X天
    ma20IncreaseX = Column('ma20IncreaseX', Integer, default=0)  # 20日均线连续上涨X天
    priceBreak_20 = Column('priceBreak_20', Integer, default=0)  # 收盘价上探20日均线
    ma60IncreaseX = Column('ma60IncreaseX', Integer, default=0)  # 60日均线连续上涨X天
    priceBreak_60 = Column('priceBreak_60', Integer, default=0)  # 收盘价上探60日均线
    priceBack_60 = Column('priceBack_60', Integer, default=0)  # 收盘价回踩60日均线
    sarGreenToRed = Column('sarGreenToRed', Integer, default=0)  # SAR由红转绿
    sarRedToGreen = Column('sarRedToGreen', Integer, default=0)  # SAR由绿转红
    sarRedX = Column('sarRedX', Integer, default=0)  # SAR持续X日红线
    sarGreenX = Column('sarGreenX', Integer, default=0)  # SAR持续X日绿线
    kdjOverBuy = Column('kdjOverBuy', Integer, default=0)  # KDJ超买
    kdjOverSell = Column('kdjOverSell', Integer, default=0)  # KDJ超卖
    bollCrossAboveLowBand = Column('bollCrossAboveLowBand', Integer, default=0)  # 上穿BOLL上轨
    bollCrossBelowUpperBand = Column('bollCrossBelowUpperBand', Integer, default=0)  # 下穿BOLL上轨
    rsiOverBuy = Column('rsiOverBuy', Integer, default=0)  # RSI超买
    rsiOverSell = Column('rsiOverSell', Integer, default=0)  # RSI超卖
    rsiBuyToSell = Column('rsiBuyToSell', Integer, default=0)  # RSI超买反转
    rsiSellToBuy = Column('rsiSellToBuy', Integer, default=0)  # RSI超卖反转
    rsiUpAbove50 = Column('rsiUpAbove50', Integer, default=0)  # RSI由底部上升到50以上
    rsiDownBelow50 = Column('rsiDownBelow50', Integer, default=0)  # RSI由顶部下穿到50以下
    wrOverSell = Column('wrOverSell', Integer, default=0)  # WR超卖
    wrOverBuy = Column('wrOverBuy', Integer, default=0)  # WR超买
    wrUpAbove50 = Column('wrUpAbove50', Integer, default=0)  # WR指标上穿50
    wrDownBelow50 = Column('wrDownBelow50', Integer, default=0)  # WR指标下穿50
    wrBuyToSell = Column('wrBuyToSell', Integer, default=0)  # WR超卖反转
    wrSellToBuy = Column('wrSellToBuy', Integer, default=0)  # WR超买反转
    pdiUpCrossMdi = Column('pdiUpCrossMdi', Integer, default=0)  # PDI上穿MDI
    pdiAboveMdi = Column('pdiAboveMdi', Integer, default=0)  # PDI高于MDI
    pdiBelowMdi = Column('pdiBelowMdi', Integer, default=0)  # PDI低于MDI
    adxHighToLow = Column('adxHighToLow', Integer, default=0)  # ADX高位拐向
    adxBelow20 = Column('adxBelow20', Integer, default=0)  # ADX20以下
    psyAbove75 = Column('psyAbove75', Integer, default=0)  # PSY高于75
    psyBelow25 = Column('psyBelow25', Integer, default=0)  # PSY低于25
    psyHighToLow75 = Column('psyHighToLow75', Integer, default=0)  # PSY高位区间回落75以下
    psyLowToHigh25 = Column('psyLowToHigh25', Integer, default=0)  # PSY低位区间反弹25以上
    adtmAboveP5 = Column('adtmAboveP5', Integer, default=0)  # ADTM高于0.5
    adtmAboveN5 = Column('adtmAboveN5', Integer, default=0)  # ADTM低于-0.5
    adtmGoldenCross = Column('adtmGoldenCross', Integer, default=0)  # ADTM-0.5以下金叉
    adtmDeadCross = Column('adtmDeadCross', Integer, default=0)  # ADTM0.5以上死叉
    dmaGoldenCross = Column('dmaGoldenCross', Integer, default=0)  # DMA金叉
    dmaDeadCross = Column('dmaDeadCross', Integer, default=0)  # DMA死叉
    dmaUpZeroGoldenCross = Column('dmaUpZeroGoldenCross', Integer, default=0)  # DMA零轴以上金叉
    dmaDownZeroGoldenCross = Column('dmaDownZeroGoldenCross', Integer, default=0)  # DMA零轴以下金叉
    rocOverBuy = Column('rocOverBuy', Integer, default=0)  # ROC超买区间
    rocOverSell = Column('rocOverSell', Integer, default=0)  # ROC超卖区间
    rocUpZeroGoldenCross = Column('rocUpZeroGoldenCross', Integer, default=0)  # ROC零轴以上金叉
    rocDownZeroGoldenCross = Column('rocDownZeroGoldenCross', Integer, default=0)  # ROC零轴以下金叉
    biasBelow0 = Column('biasBelow0', Integer, default=0)  # BIAS-12零轴以下
    biasUpper0 = Column('biasUpper0', Integer, default=0)  # BIAS-12零轴以上
    biasToPositive = Column('biasToPositive', Integer, default=0)  # BIAS正向乖离
    biasToNegative = Column('biasToNegative', Integer, default=0)  # BIAS负向乖离
    bias24UpTo6 = Column('bias24UpTo6', Integer, default=0)  # BIAS-24日线上穿BIAS-6日线
    bias24DownTo6 = Column('bias24DownTo6', Integer, default=0)  # BIAS-6日线下穿BIAS-24日线
    asiMA10GoldenCross = Column('asiMA10GoldenCross', Integer, default=0)  # ASI与10日均线金叉
    asiMA10DeadCross = Column('asiMA10DeadCross', Integer, default=0)  # ASI与10日均线死叉
    asiBreakPreHigh = Column('asiBreakPreHigh', Integer, default=0)  # ASI突破前次高点
    asiBreakPreLow = Column('asiBreakPreLow', Integer, default=0)  # ASI跌破前次低点
    wvadUpCrossZero = Column('wvadUpCrossZero', Integer, default=0)  # WVAD上穿零轴
    wvadDownCrossZero = Column('wvadDownCrossZero', Integer, default=0)  # WVAD下穿零轴
    wvadUpZeroGoldCross = Column('wvadUpZeroGoldCross', Integer, default=0)  # WVAD0轴以上金叉
    wvadDownZeroGoldCross = Column('wvadDownZeroGoldCross', Integer, default=0)  # WVAD0轴以下金叉
    wvadUpZeroDeadCross = Column('wvadUpZeroDeadCross', Integer, default=0)  # WVAD零轴以上死叉
    wvadDownZeroDeadCross = Column('wvadDownZeroDeadCross', Integer, default=0)  # WVAD0轴以上死叉
    obvNewHigh = Column('obvNewHigh', Integer, default=0)  # OBV新高
    obvNewLow = Column('obvNewLow', Integer, default=0)  # OBV低点
    obvBullishDivergences = Column('obvBullishDivergences', Integer, default=0)  # OBV顶背离
    obvBearishDivergences = Column('obvBearishDivergences', Integer, default=0)  # OBV底背离
    mtmUpCrossMtmma = Column('mtmUpCrossMtmma', Integer, default=0)  # MTM上穿MTMMA
    mtmDownCrossMtmma = Column('mtmDownCrossMtmma', Integer, default=0)  # MTM下穿MTMMA
    mtmBullishDivergences = Column('mtmBullishDivergences', Integer, default=0)  # MTM股价高位背离
    mtmBearishDivergences = Column('mtmBearishDivergences', Integer, default=0)  # MTM股价低位背离
    mtmCloseLowUp = Column('mtmCloseLowUp', Integer, default=0)  # MTM和股价低位上升
    mtmCloseHighDown = Column('mtmCloseHighDown', Integer, default=0)  # MTM和股价高位下降
    crLt50 = Column('crLt50', Integer, default=0)  # CR小于50
    crGt400 = Column('crGt400', Integer, default=0)  # CR大于400
    crHighPosDownCrossMa10 = Column('crHighPosDownCrossMa10', Integer, default=0)  # CR高位下穿CRMAC10日均线
    crUpCrossFourMa = Column('crUpCrossFourMa', Integer, default=0)  # CR连续上穿4根均线
    crDownCrossFourMa = Column('crDownCrossFourMa', Integer, default=0)  # CR回落到4根均线以下
    expmaUpCross = Column('expmaUpCross', Integer, default=0)  # EXPMA向上交叉
    expmaDownCross = Column('expmaDownCross', Integer, default=0)  # EXPMA向下交叉
    exp1GtExp2 = Column('exp1GtExp2', Integer, default=0)  # EXP1大于EXP2
    exp1LtExp2 = Column('exp1LtExp2', Integer, default=0)  # EXP1小于EXP2
    expmaMaLongSpeedUp = Column('expmaMaLongSpeedUp', Integer, default=0)  # EXPMA多头进攻
    expmaMaShortSpeedUp = Column('expmaMaShortSpeedUp', Integer, default=0)  # EXPMA空头进攻
    expmaMaLongTop = Column('expmaMaLongTop', Integer, default=0)  # EXPMA多头回撤
    expmaMaShortTop = Column('expmaMaShortTop', Integer, default=0)  # EXPMA空头回撤
    closeDownCrossBbi = Column('closeDownCrossBbi', Integer, default=0)  # 收盘价自上向下穿越BBI
    closeUpCrossBbi = Column('closeUpCrossBbi', Integer, default=0)  # 收盘价自下向上穿越BBI
    closeGtBbi = Column('closeGtBbi', Integer, default=0)  # 股价高于BBI
    closeLtBbi = Column('closeLtBbi', Integer, default=0)  # 股价低于BBI
    closeTouchXtTop = Column('closeTouchXtTop', Integer, default=0)  # 收盘价触及上顶边
    closeTouchXtBottom = Column('closeTouchXtBottom', Integer, default=0)  # 收盘价触及下底边
    closeUpCrossXtTop = Column('closeUpCrossXtTop', Integer, default=0)  # 收盘价上穿箱体顶边
    closeGtXtTop = Column('closeGtXtTop', Integer, default=0)  # 收盘价高于箱体顶边
    closeBackIntoXt = Column('closeBackIntoXt', Integer, default=0)  # 收盘价回返箱体之内
    closeDownCrossXtBottom = Column('closeDownCrossXtBottom', Integer, default=0)  # 收盘价下穿箱体底边
    aUpCrossJ = Column('aUpCrossJ', Integer, default=0)  # A线上穿J线
    aDownCrossJ = Column('aDownCrossJ', Integer, default=0)  # A线下穿J线
    aAboveJ = Column('aAboveJ', Integer, default=0)  # A线位于J线之上
    aBelowJ = Column('aBelowJ', Integer, default=0)  # A线位于J线之下
    closeGtAandAGtJ = Column('closeGtAandAGtJ', Integer, default=0)  # 收盘价高于A线，A线高于J线
    closeLtAandALtJ = Column('closeLtAandALtJ', Integer, default=0)  # 收盘价低于A线，A线低于J线
    pubuMaLong = Column('pubuMaLong', Integer, default=0)  # 瀑布线多头发散
    pubuMaShort = Column('pubuMaShort', Integer, default=0)  # 瀑布线空头发散
    pubuBullishDivergences = Column('pubuBullishDivergences', Integer, default=0)  # 瀑布线顶部背离
    pubuBearishDivergences = Column('pubuBearishDivergences', Integer, default=0)  # 瀑布线底部收敛
    pubuDull = Column('pubuDull', Integer, default=0)  # 瀑布线无序盘整
    closeUpCrossPubu = Column('closeUpCrossPubu', Integer, default=0)  # 单日股价低位上穿瀑布线
    closeDownCrossPubu = Column('closeDownCrossPubu', Integer, default=0)  # 单日股价高位下穿瀑布线
    xtBreak = Column('xtBreak', Integer, default=0)  # 箱体突破形态
    dmaUpZeroGoldenCrossX = Column('dmaUpZeroGoldenCrossX', Integer, default=0)  # DMA零轴以上X次金叉
    dmaDownZeroGoldenCrossX = Column('dmaDownZeroGoldenCrossX', Integer, default=0)  # DMA零轴以下X次金叉
    morningStar = Column('morningStar', Integer, default=0)  # 启明星
    eveningStar = Column('eveningstar', Integer, default=0)  # 黄昏星
    leaveBigline = Column('leaveBigline', Integer, default=0)  # 高开大阴线
    lineOpened = Column('lineOpened', Integer, default=0)  # 低开大阳线
    threeYin = Column('threeYin', Integer, default=0)  # 三连阳
    threeYang = Column('threeYang', Integer, default=0)  # 三连阳
    breakFourRules = Column('breakFourRules', Integer, default=0)  # 四周突破法则
    breakAwayHigh = Column('breakAwayHigh', Integer, default=0)  # 跳空缺口高开
    breakAwayLow = Column('breakAwayLow', Integer, default=0)  # 跳空缺口低开
    darkcloudOverhead = Column('darkcloudOverhead', Integer, default=0)  # 乌云盖顶形态

    platformFinishing = Column('platformFinishing', Integer, default=0)  # 平台整理
    strongArrangement = Column('strongArrangement', Integer, default=0)  # 强势整理
    vReverse = Column('vReverse', Integer, default=0)  # V形反转
    uReverse = Column('uReverse', Integer, default=0)  # U形反转
    nRise = Column('nRise', Integer, default=0)  # N字上升形态

    create_time = Column('create_time', DateTime, default=datetime.datetime.now)  # 创建时间
    update_time = Column('update_time', DateTime, default=datetime.datetime.now)  # 更新时间


    code = Column('code', String(20), primary_key=True)  # 股票代码
    date = Column('date', Date, index=True, primary_key=True)  # 最新日期
    pe = Column('pe', Numeric(20, 4))  # 市盈率
    pb = Column('pb', Numeric(20, 4))  # 市净率
    ps = Column('ps', Numeric(20, 4))  # 市销率
    pe_ttm = Column('pe_ttm', Numeric(20, 4))  # 动态PE（TTM）
    ps_ttm = Column('ps_ttm', Numeric(20, 4))  # 动态PS（TTM）
    popcf = Column('popcf', Numeric(20, 4))  # 经营市现率TTM
    totalmarketvalue = Column('totalmarketvalue', Numeric(20, 4))  # 总市值
    tfc = Column('tfc', Numeric(20, 4))  # 流通市值
    totals = Column('totals', Numeric(20, 4))  # 总股本
    dpr = Column('dpr', Numeric(20, 4))  # 股息率
    gross_profit_rate = Column('gross_profit_rate', Numeric(20, 4))  # 销售净利率
    opm = Column('opm', Numeric(20, 4))  # 经营利润率
    npm = Column('npm', Numeric(20, 4))  # 净利润率
    net_profits = Column('net_profits', Numeric(20, 4))  # 净利润
    revg = Column('revg', Numeric(20, 4))  # 销售增长率 营业收入增长率
    nprg = Column('nprg', Numeric(20, 4))  # 净利润增长率
    roaa = Column('roaa', Numeric(20, 4))  # 总资产收益率
    roe = Column('roe', Numeric(20, 4))  # 净资产收益率
    roic = Column('roic', Numeric(20, 4))  # 资本回报率
    epsg = Column('epsg', Numeric(20, 4))  # 每股收益增长率
    eps = Column('eps', Numeric(20, 8))  # 基本每股收益
    perundp = Column('perundp', Numeric(20, 4))  # 每股未分配利润
    ebits = Column('ebits', Numeric(20, 4))  # 每股息税前利润
    bvps = Column('bvps', Numeric(20, 4))  # 每股净资产
    srs = Column('srs', Numeric(20, 4))  # 每股盈余公积
    per_st_profit = Column('per_st_profit', Numeric(20, 4))  # 每股股利
    lde = Column('lde', Numeric(20, 4))  # 负债权益比
    ytototalcap = Column('ytototalcap', Numeric(20, 4))  # 归属于母公司的所有者权益
    em = Column('em', Numeric(20, 4))  # 权益乘数
    eamig = Column('eamig', Numeric(20, 4))  # 归属母公司股东权益增长率
    opcfs = Column('opcfs', Numeric(20, 4))  # 每股经营现金流
    opcfg = Column('opcfg', Numeric(20, 4))  # 经营活动现金净流量增长率
    reservedPerShare = Column('reservedPerShare', Numeric(20, 4))  # 每股公积金
    epcf = Column('epcf', Numeric(20, 4))  # 每股现金流
    close = Column('close', Numeric(20, 4))  # 收盘价
    ashares_trade = Column('ashares_trade', Numeric(20, 4))  # A股流通股本
    total_revenue = Column('total_revenue', Numeric(20, 4))  # 营业总收入
    operation_profit = Column('operation_profit', Numeric(20, 4))  # 营业利润
    cfo = Column('cfo', Numeric(20, 4))  # 经营性现金流净值
    cfo_netincome = Column('cfo_netincome', Numeric(20, 4))  # 净利润现金含量
    ipo_date = Column('ipo_date', DateTime)
    s_qfa_yoynetprofit = Column('s_qfa_yoynetprofit', Numeric(20, 4))  # 单季度归母净利润增长率（TTM）


    cashratio = Column('cashratio', Numeric(20, 4), index=True)  # 现金比率
    cf_sales = Column('cf_sales', Numeric(20, 4), index=True)  # 销售现金比率
    cf_sales = 经营现金净流量对销售收入比率
    arturnover = Column('arturnover', Numeric(20, 4), index=True)  # 应收账款周转率
    inventory_turnover = Column('inventory_turnover', Numeric(20, 4), index=True)  # 存货周转率
    currentasset_turnover = Column('currentasset_turnover', Numeric(20, 4), index=True)  # 流动资产周转率

    business_taxes = Column('business_taxes', Numeric(20, 4))  # 营业税金及附加
    total_operating_costs = Column('total_operating_costs', Numeric(20, 4))  # 营业总成本
    operating_costs = Column('operating_costs', Numeric(20, 4))  # 营业成本
    sales_expenses = Column('sales_expenses', Numeric(20, 4))  # 销售费用
    net_financial_expenses = Column('net_financial_expenses', Numeric(20, 4))  # 财务费用
    pre_tax_profit = Column('pre_tax_profit', Numeric(20, 4))  # 利润总额
    pre_tax_profit = 税前利润
    af_tax_profit = Column('af_tax_profit', Numeric(20, 4))  # 税后利润

    incf = Column('incf', Numeric(20, 4))  # 投资活动产生的现金流量净额
    cpte = Column('cpte', Numeric(20, 4))  # 支付给职工以及为职工支付的现金
    fncf = Column('fncf', Numeric(20, 4))  # 筹资活动产生的现金流量净额
    cate = Column('cate', Numeric(20, 4))  # 期末现金及现金等价物余额

    cash_equivalents = Column('cash_equivalents', Numeric(20, 4))  # 货币资金
    financial_assets_at_fair_value = Column('financial_assets_at_fair_value', Numeric(20, 4))  # 交易性金融资产
    accounts_receivable = Column('accounts_receivable', Numeric(20, 4))  # 应收账款
    inventories = Column('inventories', Numeric(20, 4))  # 存货
    total_current_assets = Column('total_current_assets', Numeric(20, 4))  # 流动资产合计
    fixed_property = Column('fixed_property', Numeric(20, 4))  # 固定资产
    intangible_assets = Column('intangible_assets', Numeric(20, 4))  # 无形资产
    goodwill = Column('goodwill', Numeric(20, 4))  # 商誉
    total_assets = Column('total_assets', Numeric(20, 4))  # 资产总计/资产总额
    capital_reserve = Column('capital_reserve', Numeric(20, 4))  # 资本公积金
    surplus_reserve = Column('surplus_reserve', Numeric(20, 4))  # 盈余公积金
    retained_earnings = Column('retained_earnings', Numeric(20, 4))  # 未分配利润
    total_equity = Column('total_equity', Numeric(20, 4))  # 股东权益合计
    invest_real_estate = Column('invest_real_estate', Numeric(20, 4))  # 投资性房地产
    tot_non_cur_assets = Column('tot_non_cur_assets', Numeric(20, 4))  # 非流动资产合计
    tot_cur_liab = Column('tot_cur_liab', Numeric(20, 4))  # 流动负债合计
    specific_item_payable = Column('specific_item_payable', Numeric(20, 4))  # 专项应付款
    tot_non_cur_liab = Column('tot_non_cur_liab', Numeric(20, 4))  # 非流动负债合计

    tot_liab = Column('tot_liab', Numeric(20, 4))  # 负债合计
    special_rsrv = Column('special_rsrv', Numeric(20, 4))  # 专项储备
    tot_shrhldr_eqy_incl_min_int = Column('tot_shrhldr_eqy_incl_min_int', Numeric(20, 4))  # 所有者权益合计
    oper_rev = Column('oper_rev', Numeric(20, 4))  # 营业收入
    less_gerl_admin_exp = Column('less_gerl_admin_exp', Numeric(20, 4))  # 管理费用
    inc_tax = Column('inc_tax', Numeric(20, 4))  # 所得税费用
    cash_recp_sg_and_rs = Column('cash_recp_sg_and_rs', Numeric(20, 4))  # 销售商品、提供劳务收到的现金
    other_cash_recp_ral_oper_act = Column('other_cash_recp_ral_oper_act', Numeric(20, 4))  # 收到其他与经营活动有关的现金
    stot_cash_inflows_oper_act = Column('stot_cash_inflows_oper_act', Numeric(20, 4))  # 经营活动现金流入小计
    stot_cash_outflows_oper_act = Column('stot_cash_outflows_oper_act', Numeric(20, 4))  # 经营活动现金流出小计
    net_cash_flows_oper_act = Column('net_cash_flows_oper_act', Numeric(20, 4))  # 经营活动产生的现金流量净额
    stot_cash_inflows_inv_act = Column('stot_cash_inflows_inv_act', Numeric(20, 4))  # 投资活动现金流入小计
    stot_cash_outflows_inv_act = Column('stot_cash_outflows_inv_act', Numeric(20, 4))  # 投资活动现金流出小计
    stot_cash_inflows_fnc_act = Column('stot_cash_inflows_fnc_act', Numeric(20, 4))  # 筹资活动现金流入小计
    stot_cash_outflows_fnc_act = Column('stot_cash_outflows_fnc_act', Numeric(20, 4))  # 筹资活动现金流出小计
    net_incr_cash_cash_equ = Column('net_incr_cash_cash_equ', Numeric(20, 4))  # 现金及现金等价物净增加额
    s_fa_grossprofitmargin = Column('s_fa_grossprofitmargin', Numeric(20, 4))  # 销售毛利率
    s_fa_netprofitmargin = Column('s_fa_netprofitmargin', Numeric(20, 4))  # 销售净利率
    s_fa_cfps = Column('s_fa_cfps', Numeric(20, 4))  # 每股现金流量净额
    s_fa_arturn = Column('s_fa_arturn', Numeric(20, 4))  # 应收账款周转率
    s_fa_arturndays = Column('s_fa_arturndays', Numeric(20, 4))  # 应收账款周转天数
    s_fa_invturn = Column('s_fa_invturn', Numeric(20, 4))  # 应收账款周转天数
    s_fa_invturndays = Column('s_fa_invturndays', Numeric(20, 4))  # 存货周转天数
    s_fa_caturn = Column('s_fa_caturn', Numeric(20, 4))  # 流动资产周转率
    s_fa_faturn = Column('s_fa_faturn', Numeric(20, 4))  # 固定资产周转率
    s_fa_assetsturn = Column('s_fa_assetsturn', Numeric(20, 4))  # 总资产周转率
    s_fa_current = Column('s_fa_current', Numeric(20, 4))  # 流动比率
    s_fa_quick = Column('s_fa_quick', Numeric(20, 4))  # 速动比率
    s_fa_debttoequity = Column('s_fa_debttoequity', Numeric(20, 4))  # 产权比率
    s_fa_ebittointerest = Column('s_fa_ebittointerest', Numeric(20, 4))  # 利息保障倍数

    intangible_per_total = Column('intangible_per_total', Numeric(20, 4))  # 无形资产比率
    fixed_per_total = Column('fixed_per_total', Numeric(20, 4))  # 固定资产比率
    non_cur_per_total = Column('non_cur_per_total', Numeric(20, 4))  # 非流动资产比率
    op_capital = Column('op_capital', Numeric(20, 4))  # 营运资本（万元）
    shareholder_per_assets = Column('shareholder_per_assets', Numeric(20, 4))  # 股东权益比率
    capitalized_ratio = Column('capitalized_ratio', Numeric(20, 4))  # 资本化比率
    equity_per_fixed = Column('equity_per_fixed', Numeric(20, 4))  # 股东权益与固定资产比率
    s_fa_yoy_equity = Column('s_fa_yoy_equity', Numeric(20, 4))  # 净资产增长率
    s_fa_yoyop = Column('s_fa_yoyop', Numeric(20, 4))  # 营业利润增长率
    tot_liab_shrhldr_eqy = Column('tot_liab_shrhldr_eqy', Numeric(20, 4))  # 负债和所有者权益总计
    profit_per_cost = Column('profit_per_cost', Numeric(20, 4))  # 成本费用利润率
    net_profit_per_cost = Column('net_profit_per_cost', Numeric(20, 4))  # 成本费用净利润率
    cash_per_cur_liab = Column('cash_per_cur_liab', Numeric(20, 4))  # 现金流动负债比率
    tangible_per_tot_liab = Column('tangible_per_tot_liab', Numeric(20, 4))  # 有形净值债务率
    tot_liab_net_ocf = Column('tot_liab_net_ocf', Numeric(20, 4))  # 偿债保障比率
    equity_per_asset = Column('equity_per_asset', Numeric(20, 4))  # 股东权益比率
    asset_per_liab = Column('asset_per_liab', Numeric(20, 4))  # 资产负债比率
    long_liab_per_asset = Column('long_liab_per_asset', Numeric(20, 4))  # 长期负债比率
    s_fa_cash = Column('s_fa_cash', Numeric(20, 4))  # 现金比率

    ebit_per_avg_tot_asset = Column('ebit_per_avg_tot_asset', Numeric(20, 4))  # 基本收益率
    tot_profit_per_avg_tot_asset = Column('tot_profit_per_avg_tot_asset', Numeric(20, 4))  # 资产利润率
    div_payout_ratio = Column('div_payout_ratio', Numeric(20, 4))  # 股利支付率
    retention_ratio = Column('retention_ratio', Numeric(20, 4))  # 收益留存率
    tot_asset_growth_rate = Column('tot_asset_growth_rate', Numeric(20, 4))  # 总资产增长率
    sustainable_growth_rate = Column('sustainable_growth_rate', Numeric(20, 4))  # 可持续增长率
    liab_per_equity = Column('liab_per_equity', Numeric(20, 4))  # 资本负债比率
    ocf_per_tot_cf = Column('ocf_per_tot_cf', Numeric(20, 4))  # 净现金流量结构分析
    principal_interest_repayment_ratio = Column('principal_interest_repayment_ratio', Numeric(20, 4))  # （到期债务）本息偿付比率

    notes_rcv = Column('notes_rcv', Numeric(20, 4))  # 应收票据
    prepay = Column('prepay', Numeric(20, 4))  # 预付款项
    oth_rcv = Column('oth_rcv', Numeric(20, 4))  # 其他应收款
    non_cur_assets_due_within_1y = Column('non_cur_assets_due_within_1y', Numeric(20, 4))  # 一年内到期的非流动资产
    oth_cur_assets = Column('oth_cur_assets', Numeric(20, 4))  # 其他流动资产
    long_term_eqy_invest = Column('long_term_eqy_invest', Numeric(20, 4))  # 长期股权投资
    long_term_rec = Column('long_term_rec', Numeric(20, 4))  # 长期应收款
    r_and_d_costs = Column('r_and_d_costs', Numeric(20, 4))  # 开发支出
    st_borrow = Column('st_borrow', Numeric(20, 4))  # 短期借款
    notes_payable = Column('notes_payable', Numeric(20, 4))  # 应付票据
    acct_payable = Column('acct_payable', Numeric(20, 4))  # 应付账款
    adv_from_cust = Column('adv_from_cust', Numeric(20, 4))  # 预收款项
    empl_ben_payable = Column('empl_ben_payable', Numeric(20, 4))  # 应付职工薪酬
    taxes_surcharges_payable = Column('taxes_surcharges_payable', Numeric(20, 4))  # 应交税费
    int_payable = Column('int_payable', Numeric(20, 4))  # 应付利息
    dvd_payable = Column('dvd_payable', Numeric(20, 4))  # 应付股利
    oth_payable = Column('oth_payable', Numeric(20, 4))  # 其他应付款
    non_cur_liab_due_within_1y = Column('non_cur_liab_due_within_1y', Numeric(20, 4))  # 一年内到期的非流动负债
    oth_cur_liab = Column('oth_cur_liab', Numeric(20, 4))  # 其他流动负债
    lt_borrow = Column('lt_borrow', Numeric(20, 4))  # 长期借款
    bonds_payable = Column('bonds_payable', Numeric(20, 4))  # 应付债券
    lt_payable = Column('lt_payable', Numeric(20, 4))  # 长期应付款
    lt_payroll_payable = Column('lt_payroll_payable', Numeric(20, 4))  # 长期应付职工薪酬
    cap_stk = Column('cap_stk', Numeric(20, 4))  # 股本
    other_comp_income = Column('other_comp_income', Numeric(20, 4))  # 其他综合收益
    cash_pay_goods_purch_serv_rec = Column('cash_pay_goods_purch_serv_rec', Numeric(20, 4))  # 购买商品、接受劳务支付的现金
    pay_all_typ_tax = Column('pay_all_typ_tax', Numeric(20, 4))  # 支付的各项税费
    other_cash_pay_ral_oper_act = Column('other_cash_pay_ral_oper_act', Numeric(20, 4))  # 支付其他与经营活动有关的现金
    cash_recp_disp_withdrwl_invest = Column('cash_recp_disp_withdrwl_invest', Numeric(20, 4))  # 收回投资收到的现金
    cash_recp_return_invest = Column('cash_recp_return_invest', Numeric(20, 4))  # 取得投资收益收到的现金
    cash_pay_acq_const_fiolta = Column('cash_pay_acq_const_fiolta', Numeric(20, 4))  # 购建固定资产、无形资产和其他长期资产支付的现金
    cash_paid_invest = Column('cash_paid_invest', Numeric(20, 4))  # 投资支付的现金
    cash_recp_cap_contrib = Column('cash_recp_cap_contrib', Numeric(20, 4))  # 吸收投资收到的现金
    cash_recp_borrow = Column('cash_recp_borrow', Numeric(20, 4))  # 取得借款收到的现金
    proc_issue_bonds = Column('proc_issue_bonds', Numeric(20, 4))  # 发行债券收到的现金
    cash_prepay_amt_borr = Column('cash_prepay_amt_borr', Numeric(20, 4))  # 偿还债务支付的现金
    cash_pay_dist_dpcp_int_exp = Column('cash_pay_dist_dpcp_int_exp', Numeric(20, 4))  # 分配股利、利润或偿付利息支付的现金
    cash_cash_equ_beg_period = Column('cash_cash_equ_beg_period', Numeric(20, 4))  # 期初现金及现金等价物余额
    cash_cash_equ_end_period = Column('cash_cash_equ_end_period', Numeric(20, 4))  # 期末现金及现金等价物余额
    fin_assets_avail_for_sale = Column('fin_assets_avail_for_sale', Numeric(20, 4))  # 可供出售金融资产
    impairtog = Column('impairtog', Numeric(20, 4))  # 资产减值损失
    plus_non_oper_rev = Column('plus_non_oper_rev', Numeric(20, 4)) # 营业外收入
    less_non_oper_exp = Column('less_non_oper_exp', Numeric(20, 4)) # 营业外支出
    create_time = Column('create_time', DateTime, default=datetime.datetime.now)  # 创建时间
    update_time = Column('update_time', DateTime, default=datetime.datetime.now)  # 更新时间
